name: Settings Provider Key Status Truth

on:
  pull_request:
    branches: [feature/product-local-cortex]
    paths:
      - 'android/app/src/main/java/com/jarvis/mobile/DeveloperSettingsActivity.java'
      - '.github/scripts/settings-provider-key-status-contract.sh'
      - '.github/workflows/settings-provider-key-status.yml'
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Verify provider key clearing refreshes visible runtime status
        run: sh .github/scripts/settings-provider-key-status-contract.sh
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'
      - name: Compile Developer Settings production surface
        working-directory: android
        run: gradle --rerun-tasks :app:compileDebugJavaWithJavac --stacktrace
