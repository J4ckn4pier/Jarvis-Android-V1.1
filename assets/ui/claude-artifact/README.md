# Canonical Claude JARVIS UI artifact

Authoritative live prototype:

- Artifact: `https://claude.ai/code/artifact/a7271792-25ce-4ebe-a24c-ffbd65680e4d`
- Expected source filename: `jarvis-live.html`
- Technology: HTML + CSS + JavaScript (not Java)
- Approximate source size recorded by the UI resource manifest: ~220 KB

This directory is the permanent repository location for the exported artifact source and any supporting files. The live Artifact remains the visual/UX source of truth until an exact export of `jarvis-live.html` is committed here.

## Import rule

Do not recreate or approximate the Artifact from memory. When the exact Artifact source is available, store it here byte-for-byte as `jarvis-live.html` (or split CSS/JS only if preserving behavior exactly), then use that committed source as the Android UI integration reference.

## Current access note

The project Google Drive contains the canonical icon resources and the Artifact URL, but not the ~220 KB Artifact source itself. The current GitHub/Drive connectors cannot extract source from the private Claude code-artifact endpoint. This is intentionally documented rather than substituting a guessed UI implementation.
