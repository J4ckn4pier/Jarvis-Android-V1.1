package com.jarvis.brain;

/** Stable backend sections backing the prototype's list/state surfaces. */
public enum UiSection {
    TASKS,
    PROJECTS,
    ROUTINES,
    CALENDAR,
    MESSAGES,
    BROWSER,
    SKILLS
}
