package com.jarvis.brain;

/** Cost class used to preserve JARVIS's zero-surprise-cost routing policy. */
public enum ProviderTier {
    FREE_LOCAL,
    INCLUDED,
    FREE_EXTERNAL,
    PAID_EXTERNAL
}
